{{- if .Values.tls.enabled }}
{{- if .Values.tls.selfSigned.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vault.fullname" . }}-tls
  labels:
    {{- include "vault.labels" . | nindent 4 }}
type: kubernetes.io/tls
data:
  # These will be generated by a job or init container
  # For now, they are placeholders
  tls.crt: ""
  tls.key: ""
  ca.crt: ""
{{- end }}
{{- end }} 